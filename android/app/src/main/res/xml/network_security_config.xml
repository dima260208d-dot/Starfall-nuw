<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Game server / matchmaker (HTTP + WS). Prefer HTTPS on VPS for store review. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">217.60.245.116</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
